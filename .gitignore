# Reference / scratch files
reference_*

# Secrets / env
.env
.env.*
*.pem
*.key

# Per-tenant worker private keys for the sealed-box secret channel (NEVER commit)
tenants/*/.worker_key

# Tenant MCP configs (URLs embed session tokens — never commit) + active symlink at repo root
tenants/*/.mcp.json
/.mcp.json

# WhatsApp — session DB / runtime state (never commit). WhatsApp now goes through
# the OpenWA gateway (../OpenWA); the old in-repo whatsapp-mcp bridge was removed.
*.db
tenants/*/whatsapp_sync_state.json

# Best Roadways control-panel runtime + uploaded lead data (PII — never commit)
tenants/best-roadways/cycle.log
tenants/best-roadways/panel_settings.json
tenants/best-roadways/leads.csv
tenants/best-roadways/enriched_leads.json
tenants/best-roadways/qualify_buckets.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
.vercel
