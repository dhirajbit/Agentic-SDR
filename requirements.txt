# Core SDR
requests>=2.31
python-dotenv>=1.0

# Cloud sync layer (cloud_sync.py / cloud_agent.py)
psycopg[binary,pool]>=3.2
pynacl>=1.5

# Tenant-specific (best-roadways drafting) — install where needed
# google-genai>=0.3
